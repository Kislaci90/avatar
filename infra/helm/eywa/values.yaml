# Eywa Frontend - Simplified
replicaCount: 1
image:
  registry: localhost:5000
  repository: eywa
  tag: latest
  pullPolicy: IfNotPresent
service:
  type: ClusterIP
  port: 80
serviceAccount:
  create: true
  name: eywa
env:
  - name: NODE_ENV
    value: "production"
