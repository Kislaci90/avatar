extend type Query {
    getMe: UserView
}

type Mutation {
    login(username: String!, password: String!): LoginResponse
}

type UserView {
    id: ID!,
    email: String!,
    fullName: String!,
    username: String!
}

type LoginResponse {
    token: String!,
    expiresIn: Int!,
}