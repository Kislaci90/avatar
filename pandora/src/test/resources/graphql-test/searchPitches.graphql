query searchPitches($count: Int!, $offset: Int!, $filter: PitchFilter!, $sort: String) {
    searchPitches(count: $count, offset: $offset, filter: $filter, sort:  $sort) {
        total
        pageable {
            pageNumber
            pageSize
        }
        content {
            id
            name
            properties
            pitchType
            surfaceType
            location {
                id
                name
            }
        }
    }
}